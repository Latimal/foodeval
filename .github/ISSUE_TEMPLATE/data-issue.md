---
name: Data issue
about: Report a mislabeled pair, wrong relevance grade, typo, or duplicate
labels: data
---

**Task name** (e.g. `cross_lingual_match`):

**Data version** (the task file's `version` field, or `foodeval --version`):

**Item id** (every pair, query, and item has one, like `p001` or `q014`):

**What is wrong and what it should be:**

**One-line reason** (especially for label disputes where reasonable people might differ):
