---
name: Task proposal
about: Propose a new benchmark task before writing any code
labels: task-proposal
---

**What capability it measures** and why current tasks do not already cover it:

**Task type** (retrieval / pair classification / classification, or a new metric type, which is a bigger lift):

**Where the data comes from** and that it is clean enough to license CC-BY-NC-4.0:

**Rough size** (queries / pairs / items, classes or domains):

**A handful of example records** in the matching JSON schema (see the README "Data Format" section):
